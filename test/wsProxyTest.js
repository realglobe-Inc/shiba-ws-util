/**
 * Test for wsProxy.
 * Runs with mocha.
 */
'use strict'

const wsProxy = require('../lib/wsProxy')
const { ok, equal } = require('assert')

describe('ws-proxy', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
