/**
 * Test for wsAlive.
 * Runs with mocha.
 */
'use strict'

const wsAlive = require('../lib/wsAlive')
const { ok, equal } = require('assert')

describe('ws-alive', () => {
  before(() => {
  })

  after(() => {
  })

  it('Do test', () => {

  })
})

/* global describe, before, after, it */
