'use strict'

const {dir} = require('the-templates')

module.exports = dir({
  dirname: __dirname,
  name: 'constants',
  description: 'Constants',
  cjs: true
})

if (!module.parent) {
  require('coz').render(__filename)
}